# ml-meta-perf — study documentation

Reference material for the study behind `ml-meta-perf`: why the model has the form it has, how
its terms are chosen and fitted, how it is evaluated, and what the results mean. Written
at the level of detail a paper needs, with the figures produced by `python -m ml_meta_perf`.

Chapters 0–9 are written. **Chapter 10 is not**: it is regenerated by `ml_meta_perf.report` on
every run, so the equation it analyses and the practices it states are always the ones the
current code produces. That separation is deliberate — the analysis of a fitted equation
should not depend on who reads it.

The package API is documented separately from these pages, generated with `pdoc` from the
module docstrings and published to GitHub Pages by `.github/workflows/docs.yml`. Each chapter below names the modules it describes, and
each of those modules opens with a docstring covering the same ground from the
implementation side.

## Chapters

| | | modules |
|---|---|---|
| 0 | [Related work and positioning](00-related-work.md) | — |
| 1 | [The problem and the data](01-problem.md) | `ml_meta_perf.data` |
| 2 | [Equation form and term vocabulary](02-equation-form.md) | `ml_meta_perf.terms`, `ml_meta_perf.model` |
| 3 | [Search and fitting](03-search-and-fitting.md) | `ml_meta_perf.fit`, `ml_meta_perf.analysis`, `ml_meta_perf.selection` |
| 4 | [Evaluation methodology](04-evaluation.md) | `ml_meta_perf.validate`, `ml_meta_perf.stats` |
| 5 | [Oracles and ceilings](05-oracles.md) | `ml_meta_perf.validate` |
| 6 | [Results](06-results.md) | `ml_meta_perf.experiment` |
| 7 | [From equation to evidence to practice](07-practices.md) | `ml_meta_perf.practices`, `ml_meta_perf.attribution`, `ml_meta_perf.guidance` |
| 8 | [Limitations and threats to validity](08-limitations.md) | — |
| 9 | [Model identity: the bound on better descriptors](09-model-effects.md) | `ml_meta_perf.identity` |
| 10 | [Generated report](10-report.md) | `ml_meta_perf.report`, `ml_meta_perf.guidance` |

## The one-paragraph version

`ml-meta-perf` fits `MCC = w1·t1 + … + wk·tk`, where each `t` is a simple expression over one
to three meta-features of a dataset and of a classifier, and each `w` is a real number in
the features' own units. Terms are drawn from a filtered candidate library, screened by
paired Pearson and Spearman correlation, and selected by beam search with an exact ridge
solve at every step. Three equations are contrasted — dataset features alone, model features
alone, and both — to isolate how much of MCC is attributable to model capability rather
than dataset difficulty. All three are fitted on the same 476 rows by the
same code path, so nothing but the feature set differs between them. Validation is
leave-one-dataset-out and leave-one-model-out; the inflation caused by random k-fold
splitting on grouped meta-data is quantified rather than assumed away.
