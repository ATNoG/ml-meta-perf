# The development and research environment for this repository. The examples use the
# POSIX executable paths; README.md gives their Windows PowerShell equivalents.
#
#   python3 -m venv venv
#   venv/bin/pip install -r requirements.txt
#   venv/bin/pre-commit install
#
# The project itself, editable. Everything below the blank line is development and CI
# tooling. The three static-analysis versions match `.github/workflows/main.yml` and the
# isolated environments declared in `.pre-commit-config.yaml`; pre-commit itself is pinned
# so hook orchestration is reproducible too.
-e .

ruff==0.16.1
basedpyright==1.39.8
vulture==2.16
pre-commit==4.6.2
